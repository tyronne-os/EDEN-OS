# EDEN OS — H100 Cinematic Profile (80GB VRAM)
hardware:
  name: "H100 Cinematic"
  gpu: "H100"
  vram_gb: 80

pipeline:
  target_fps: 60
  max_latency_ms: 1000

animator:
  engine: "liveportrait"
  fallback: "hunyuan"
  target_fps: 60

voice:
  tts_primary: "cosyvoice2"
  tts_fallback: "styletts2"

brain:
  primary: "anthropic"
